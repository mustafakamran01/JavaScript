/* 
    We need to Merge two arrays and remove duplicate elements.
    
    Example:
    Input: [1, 2, 3], [3, 4, 5]
    Output: [1, 2, 3, 4, 5] 
*/

let myArr1 = [1, 2, 3]
let myArr2 = [3, 4, 5]

// const newArr = myArr1.filter( (ele) => !myArr2.includes(ele))
//                      .concat(myArr2)

// console.log(newArr);


